version: '3'
services:
  nginx:
    build: ./nginx
    ports:
    - "8080:80"
    depends_on:
    - node
  node:
    build: .
    ports:
    - "4000:4000"
    volumes:
    - .:/app
    depends_on:
    - mongo
  mongo:
    image: mongo:latest
    volumes:
    - ./mongo-data:/data/db
